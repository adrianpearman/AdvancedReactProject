# import * from './generated/prisma.graphql'

type Mutation{
  createItem(
    name: String!,
    deescription: String,
    price: Int!,
    image: String,
    largeImage: String
    ) : Item!
}

type Query{
  items: [Item]!
}